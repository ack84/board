# 서버 포트 설정
server:
  port: 9090

# database 연동 설정
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/board
    driver-class-name: org.mariadb.jdbc.Driver
    username: boardset
    password: wldh2023!
  thymeleaf:
    cache: false

  # spring data jpa 설정
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update